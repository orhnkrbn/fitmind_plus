import 'package:flutter/material.dart';

class PlanDetailPage extends StatelessWidget {
  const PlanDetailPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Plan Detail')));
}
